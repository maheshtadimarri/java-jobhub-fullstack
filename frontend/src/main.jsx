import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
const API='http://localhost:8080/api';
function App(){const[jobs,setJobs]=useState([]),[q,setQ]=useState(''),[selected,setSelected]=useState(null),[form,setForm]=useState({name:'',email:''});const load=()=>fetch(`${API}/jobs?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(setJobs);useEffect(load,[]);const apply=e=>{e.preventDefault();fetch(`${API}/applications/jobs/${selected.id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}).then(r=>r.ok?alert('Application submitted'):alert('Application failed'))};return <main><header><h1>JobHub</h1><p>Find your next opportunity</p></header><section className="search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search title, company or location"/><button onClick={load}>Search</button></section><div className="grid">{jobs.map(j=><article key={j.id}><h2>{j.title}</h2><b>{j.company}</b><p>{j.location} · {j.employmentType}</p><p>{j.description}</p><button onClick={()=>setSelected(j)}>Apply</button></article>)}</div>{selected&&<div className="modal"><form onSubmit={apply}><h2>Apply for {selected.title}</h2><input required placeholder="Name" onChange={e=>setForm({...form,name:e.target.value})}/><input required type="email" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><button>Submit application</button><button type="button" onClick={()=>setSelected(null)}>Cancel</button></form></div>}</main>}createRoot(document.getElementById('root')).render(<App/>);
