spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/jobhub}
    username: ${DB_USER:jobhub}
    password: ${DB_PASSWORD:jobhub}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
server:
  port: 8080
