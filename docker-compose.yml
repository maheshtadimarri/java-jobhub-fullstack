services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: jobhub
      POSTGRES_USER: jobhub
      POSTGRES_PASSWORD: jobhub
    ports: ["5432:5432"]
    volumes: [jobhub-data:/var/lib/postgresql/data]
volumes:
  jobhub-data:
